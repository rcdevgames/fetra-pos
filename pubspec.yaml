name: pos
description: Point of Sales Fetra IT
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_bloc: ^0.21.0
  # hydrated_bloc: ^0.4.1
  cryptoutils: ^0.4.0
  indonesia: ^1.0.1
  equatable: ^0.2.0
  flutter_dotenv: ^2.0.3
  meta: ^1.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_app_host: ^0.2.2
  flutter_launcher_icons: ^0.7.3


flutter:
  uses-material-design: true
  # assets:
  #  - images/a_dot_burr.jpeg